PicturesTableIAM:
  Effect: Allow
  Action:
    - dynamodb:PutItem
    - dynamodb:Scan
    - dynamodb:GetItem
    - dynamodb:Query
    - dynamodb:UpdateItem
  Resource:
    - ${self:custom.PicturesTable.arn}
    - !Join ['/', [ '${self:custom.PicturesTable.arn}', 'index', 'tag' ]]